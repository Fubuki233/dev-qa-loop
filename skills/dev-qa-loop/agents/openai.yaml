interface:
  display_name: "Parallel Development and QA"
  short_description: "Parallel tests, task-aware QA model selection, and CI feedback"
  default_prompt: "Use $dev-qa-loop to implement this feature, choose a suitable QA model, write tests in parallel, validate snapshots, and track PR CI."

policy:
  allow_implicit_invocation: true
