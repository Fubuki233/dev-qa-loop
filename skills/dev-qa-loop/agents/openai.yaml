interface:
  display_name: "Parallel Development and QA"
  short_description: "Parallel tests, economical QA model selection, and CI feedback"
  default_prompt: "Use $dev-qa-loop to implement this feature, choose the lowest-cost capable QA model, write tests in parallel, validate snapshots, and track PR CI; justify any upgrade with evidence."

policy:
  allow_implicit_invocation: true
